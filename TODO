* Get rid of the constants module and just import those constants into their
  respective modules
* Fix the queue cooldown to take the lowest cooldown rather than the round-robin
  highest one instead
